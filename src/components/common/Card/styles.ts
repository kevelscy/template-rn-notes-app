import { styled } from '@/styles/shared'

export const WrapperCardView = styled.View({
  backgroundColor: '#DEEFFF',
  paddingVertical: 10,
  paddingHorizontal: 16,
  borderRadius: 8,
  maxWidth: 300
})

export const WrapperCardTouchable = styled.TouchableOpacity({
  backgroundColor: '#DEEFFF',
  paddingVertical: 10,
  paddingHorizontal: 16,
  borderRadius: 8,
  maxWidth: 300
})
