import { styled } from '@/styles/shared'

export const Wrapper = styled.Pressable({
  backgroundColor: '#fd2471',
  paddingVertical: 6,
  paddingHorizontal: 16,
  borderRadius: 8,
  marginVertical: 4,
  marginHorizontal: 4,
})

export const Text = styled.Text({
  color: '#ffffff',
  fontSize: 17,
  fontWeight: 'bold',
})